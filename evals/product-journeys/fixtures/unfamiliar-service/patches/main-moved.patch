--- a/service.py
+++ b/service.py	2026-09-22 12:52:46
@@ -9,7 +9,7 @@
 from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
 from pathlib import Path
 
-SESSION_TTL_SECONDS = 24 * 60 * 60  # pinned by tests/test_service.py -- do not change without a contract decision
+SESSION_TTL_SECONDS = int(os.environ.get("KEEPNOTE_TTL_SECONDS", 24 * 60 * 60))  # default pinned by tests/test_service.py; ops override via KEEPNOTE_TTL_SECONDS
 
 try:
     import markdown as _markdown
