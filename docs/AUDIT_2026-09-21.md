# Release programme audit — 2026-09-21

Incremental local code/dependency review, not an external ChatGPT audit or a
completed repository security audit. The dependency correction below does not
close the broader request/approval, project access, preview and recovery review.

## F01 — Tested and deployed dependency trees diverged

Confirmed. Docker installed `deploy/package.web.json`, whose Neutron CLI/core
pins were 0.1.4/0.1.8 while local suites used 0.2.3/0.2.2. Deployment also omitted
all pnpm security overrides. Vite/preset and pg pins differed. Testing local
routes therefore did not prove the behavior or patched dependencies deployed.
Both Docker installs resolved transitives without lockfiles; exact direct pins
did not make repeated builds reproducible.

Fix: align production direct pins with installed tested versions, carry web
security overrides into npm, commit separate production npm lockfiles and use
`npm ci` for both image installs. Seam tests compare installed versions,
overrides, lockfile roots, registry integrity and Docker's frozen installs.
CI installs both local dependency sets before those tests. Audit CI now checks
web dependencies and both production lockfiles, including the SSR toolchain.
`node scripts/audit-deployment.mjs` audits the exact committed production trees.
The remaining container/CLI inventory review is open.

## F02 — Eight current web dependency advisories

Reproduced by `pnpm --dir web audit --json`; root scan had zero. Three high and
five moderate advisory entries across five packages. Advisory count differs
from the earlier GitHub push notice because the scans were taken separately.

| Package | Fixed version selected | Exposure assessment |
|---|---|---|
| js-yaml | 3.15.2 | Transitive gray-matter content parser; vulnerable dependency confirmed. No remotely exploitable Ship YAML-input path established in this pass. |
| hono | 4.13.5 | Actual web server framework; three parser/SSG advisories. Specific parseBody/SSG exploit paths not established; patch applied rather than asserting they are reachable. |
| devalue | 5.9.2 | Framework serialization dependency; malformed-input DoS advisory. Public attacker-controlled parsing path not established here. |
| browserslist | 4.28.7 | Two advisories in build tooling; preset also ships with the SSR runtime. No public arbitrary-query/stats ingestion path established. |
| baseline-browser-mapping | 2.11.0 | Browserslist transitive invalid-input process termination. Same tooling exposure qualification. |

Authoritative records:
- https://github.com/advisories/GHSA-2883-xcg3-v3hh
- https://github.com/advisories/GHSA-gqvv-2mrq-wpjv
- https://github.com/advisories/GHSA-g6gw-c38x-mqfc
- https://github.com/advisories/GHSA-crvj-82cr-hjcx
- https://github.com/advisories/GHSA-9rgm-9g3h-6x36
- https://github.com/advisories/GHSA-c83g-rgw3-j3cx
- https://github.com/advisories/GHSA-73wf-gq98-2v4g
- https://github.com/advisories/GHSA-w5vr-8v7q-w6rv

After updating overrides, local root/web scans and separately resolved production
root/web scans report zero advisories. This is a dated registry result, not a
claim that no vulnerabilities exist. Required suites/build and live image
acceptance must pass before rollout is considered complete.

## Open review targets

- Project-scoped access: existing action grants are deployment-wide. Define the
  trusted-team boundary before promising restricted-project/guest isolation.
- Task/follow-up idempotency and lost-response recovery.
- Same-PR final merge handoff, expected-head atomicity and approval races.
- Scoped preview/attachment/websocket access and revocation.
- Durable publication/notification reconciliation and restored workspace bytes.
- Container base, bundled CLI capability/security inventory and secret lifecycle.

None of these is labeled fixed by this dependency change.
