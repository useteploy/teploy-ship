export const config = { mode: "app" };

export default function Stub() {
  return (
    <>
      <h1 class="page">Sources</h1>
      <p class="empty">Webhook sources and per-source policies (ignore / propose / auto) + budget caps — coming next in the overhaul.</p>
    </>
  );
}
