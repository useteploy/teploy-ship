export const config = { mode: "app" };

export default function Stub() {
  return (
    <>
      <h1 class="page">Spend</h1>
      <p class="empty">Per-run and per-source spend vs your daily caps — your API cost, no markup. Coming next.</p>
    </>
  );
}
