export const config = { mode: "app" };

export default function Stub() {
  return (
    <>
      <h1 class="page">Settings</h1>
      <p class="empty">Model, git tokens, sandbox, and store configuration — coming next.</p>
    </>
  );
}
